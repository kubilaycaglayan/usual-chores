Create a seed user. It could be one of the directors that we have in the chores/management/commands/seed_demo.py data. And the user's credentials, including the username or email and the password, should appear as a placeholder in the login page. Since this is a demo application, it's okay to expose that seed user's credentials because we want whoever checking this application to be easily login and check the application.
